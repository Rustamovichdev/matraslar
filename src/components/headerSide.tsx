import { Header } from "antd/es/layout/layout";
import type { FC } from "react";

const HeaderSide: FC = () => {
    return <Header style={{ padding: 0, background: "#166199" }} />;
};
export default HeaderSide;
